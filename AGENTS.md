# 巴拉丁（dsh-mobile）项目协作规则

本文件是巴拉丁项目所有 AI Agent、DSH 会话和人工开发者的强制入口。目标是防止多个会话在同一台电脑上相互覆盖代码、误提交别人改动或错误发布。

详细流程见 `docs/AI-MULTI-AGENT-DEVELOPMENT.md`。任何会写文件、运行格式化、安装依赖、提交、合并或发布的任务，开工前必须完整阅读本文件及详细流程；纯只读分析可以不创建任务分支，但不得写入。

## 1. 项目与目录

- 每次以 `git rev-parse --show-toplevel` 的实际结果确认仓库根，不凭聊天记忆猜路径。
- 当前主电脑常用仓库位置为 `<你的工作区>/balading`，但脚本和规范不得依赖这个硬编码路径。
- `web/`：React 手机/PWA 前端。
- `server/`：VPS 网关、语音、通知和资源监控。
- `android/`：Capacitor Android 工程。
- `docs/`：架构、验收和开发规范。
- `.github/workflows/build-apk.yml`：`master` 推送后的正式 APK 构建入口。
- `scripts/publish-public.ps1`：私有工作树向公共仓库复制干净快照的独立发布工具，不是普通提交工具。

## 2. 当前硬规则

1. **同一个工作目录同一时间只能有一个写入负责人。**多个会话可以并行只读，但不能同时写同一个 checkout。
2. **同一功能、bug 或文件链优先继续原会话。**不把分析、编码、测试、返工拆成多个互相不知道的写入会话。
3. **不同写入任务必须使用不同任务分支和不同 Git worktree。**只切分支但共用同一目录，不能形成隔离。
4. **并行开发，不排队。**可用分支/目录很多（当前 11 个），多个任务同时并行开发（各自占一个目录+分支）；即使文件范围重叠也各写各的，**合并时再处理冲突**。只有真正有依赖关系的任务才按顺序做。
5. **没有"解锁"概念，也没有"整个项目只允许一个写入任务"的限制。**门禁由协调器自己维护；新功能开发完、测试好，即可合并、热更。
6. **来源不明的现有改动一律保护。**不得覆盖、回退、格式化、暂存、提交、移动或删除。
7. **删除必须单独得到维护者明确批准。**包括文件、目录、配置项、测试、生成物和服务器文件；不得用清理、覆盖或移动绕过批准。
8. **默认不提交、不合并、不推送、不部署。**只有维护者明确说"提交""合并""同步""部署""发布"等对应指令时，才能执行相应动作。
9. **禁止危险 Git 操作。**禁止 `git reset --hard`、`git clean`、强推、改写公共历史、删除分支、长期依赖 stash、用一方完整覆盖合并冲突。
10. **不得用 `git add .` 或 `git add -A` 提交开发任务。**必须显式暂存当前任务文件或 hunk，并复核 staged diff。

## 3. 开工前必须检查

任何写入任务先报告并核对：

- 任务编号与一句话目标。
- 当前会话 id / 负责人。
- `git rev-parse --show-toplevel`。
- `git branch --show-current`。
- `git status --short`。
- `git worktree list`。
- 当前基线提交 `git rev-parse HEAD`。
- 允许修改的文件或目录。
- 明确不碰的范围。
- 验证命令和人工验收入口。
- 回退方式。

发现以下任一情况必须停止写入：

- 当前目录不是登记给本任务的 worktree。
- 工作区在本任务开始前已经有来源不明改动。
- 当前分支是 `master`、整合分支或其他任务分支。
- 目标文件已被其他活跃任务认领。
- 任务范围或验收条件不清楚。
- 需要删除、合并、部署或发布但没有对应授权。

## 4. 分支与 worktree 模型

正式模型：

- `master`：已经发布并可回溯的稳定版本；禁止日常直接开发。
- `integration/mobile`：候选整合分支；只由整合负责人合并和统一验证。
- `task/<任务号>-<短名称>`：单个开发任务分支。
- `release/<版本号>`：只有确实需要独立发布验证时才创建。

每个可写任务都必须拥有：

- 唯一任务分支。
- 唯一 worktree 目录。
- 唯一写入负责人。
- 明确文件范围。
- 明确验收命令。
- 最终提交号和交接记录。

当前 `publish/mobile-20260827` 及现有工作目录属于历史开发现场。在维护者批准专项整理前，不自动改名、不迁移、不合并、不清理，也不作为新并行任务模板。

## 5. 修改规则

- 修改前重新读取目标文件，不能根据旧对话内容直接整文件覆盖。
- 优先最小局部修改；确需整文件重写时，先核对当前内容和任务归属。
- 写入前后都检查目标文件是否出现非本会话改动；发现变化立即停止，重新读取并报告冲突。
- 不运行会批量改写全仓库的格式化、代码生成或依赖升级，除非任务明确批准并独占整个影响范围。
- `web/src/version.js`、`web/public/sw.js`、`web/public/version.json`、锁文件、工作流、部署脚本等共享文件默认视为高冲突文件，只能由整合/发布任务修改。
- 临时分析产物不得混入正式提交；是否删除仍需单独批准。

## 6. 测试与交付

按改动范围选择验证：

- Web：`cd web && npm test`、`cd web && npm run build`。
- Server：`cd server && npm test`。
- Android/Capacitor：按任务需要执行同步或构建；不得把本地生成物误提交。
- 玩家可见、可点击、录音、通知、后台恢复和缓存更新必须补真实手机或浏览器操作证据，自动测试不能替代。

Agent 交付时必须说明：

- 修改了哪些文件。
- 没有修改哪些受保护范围。
- 执行了哪些检查，结果如何。
- 哪些检查尚未执行。
- 当前提交号；未提交则明确写“未提交”。
- 是否需要维护者手机手测。
- 是否存在待合并、待部署或待发布动作。

Agent 只能说“内部检查通过，待维护者验收”；只有维护者可以确认“完成”。

## 7. 提交、合并和发布

### 提交

- 必须得到明确提交授权。
- 只暂存当前任务文件或 hunk。
- 提交前检查 `git diff`、`git diff --cached`、`git diff --check` 和对应测试。
- 提交信息建议：`<type>(<scope>): [任务号] <中文说明>`。

### 合并

- 必须得到明确合并授权，并由整合负责人在干净的整合 worktree 中执行。
- 合并前确认任务提交、文件范围、测试证据和冲突状态。
- 使用普通可追踪 merge；遇到冲突只做只读分析并报告，不能自动选择 ours/theirs 或整文件覆盖。
- 每次合并后重新运行整合级 Web、Server 和必要的真实操作验证。

### 发布

- 热更新、VPS 网关、APK、公共仓库快照分别是不同发布动作，授权不能互相替代。
- `master` 推送会触发 APK 工作流，因此推进 `master` 前必须明确说明影响。
- VPS 部署必须核对目标服务器、版本号、构建产物、服务状态和公网读回；不得因本地构建通过就宣称线上已更新。
- 公共快照发布前必须使用 `scripts/publish-public.ps1 -DryRun` 做敏感信息检查。

## 8. 自动门禁目标

后续工具必须实现：

- 任务登记与共享写入锁。
- worktree / 分支开工检查。
- 文件范围冲突检测。
- 文件读取版本与写入版本不一致时拒绝覆盖。
- 禁止整合分支直接开发和提交。
- Commit message、暂存范围和测试结果检查。
- 标准合并、部署、发布审计记录。
- 手机端显示负责人、分支、worktree、文件范围、状态、提交号和冲突提示。

在这些自动门禁真正上线并验证前，继续执行“全项目单写入任务”规则。
