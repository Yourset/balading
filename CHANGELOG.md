# 变更记录（CHANGELOG）

## 2026-08-26

### v0.6.0 · 底部 4 菜单 + 私人助手 + AI 绘画 + 自动更新
- 底部菜单 5→4：会话 / 私人助手 / 笔记 / AI绘画（任务、工作区、我的入口移除，解绑移入私人助手顶栏）。
- 私人助手：固定 mobile 会话 + 长期记忆自动同步 VPS；换设备自动找回原会话与历史。
- AI 绘画：固定绘画会话 + 快捷提示词；出图经 frp 8792 + 网关 /img/ 代理在手机显示。
- 自动更新根治：sw.js no-cache + SW 注册带构建时间戳 + controllerchange 自动刷新 + SW v12 策略（页面网络优先）。
- 存量设备升级入口：访问 /x 等任意路径一次即可升级。
- 修复：流式 live 残留导致回复重复显示；SPA fallback 提示下载。

## 2026-08-25

### v0.5.0 · 连接状态与文档仓库化
- 顶部连接状态指示（每秒检测：绿/黄/红 + 延迟 ms）。
- 消息顺序按 seq 排序合并（与电脑端一致）。
- 思考小条（think pill）悬浮提示。
- 初始化独立 git 仓库 + 全套 docs 文档。

### v0.4.x · 图片查看
- 消息图片缩略图（≤100KB）+ 点击全屏原图。
- md 渲染器支持图片语法；SW 附件永久缓存。

### v0.3.x · 缓存与网络
- Service Worker（静态 cache-first / API SWR / 离线）。
- localStorage 持久化列表/历史；首屏分页；mux 重连。
- 缓存头（no-cache / immutable）。

### v0.2.x · 交互体验
- 标题显示；思考隐藏；工具卡折叠/减高；系统内容过滤。
- 图片草稿（先选图后打字）；发送打断（steer）。
- 打开定位底部 / 回底按钮 / 底部跟随。

### v0.1.x · MVP
- 独立 React 前端 + 零依赖认证网关 + 设备绑定。
- 部署 m.example.com（VPS nginx/systemd + frp 隧道）。

## 2026-08-17 · 前置

- VPS 凭据与 frp 隧道（dsh.example.com 反向访问主电脑 DSH）。
